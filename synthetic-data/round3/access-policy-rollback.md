# Production Access — Process Change

Read-only prod access now works like this:

1. DM any engineer on the SRE team directly and ask them to add you to the
   `prod-readonly` group in Okta.
2. That's it. No course, no IAM request, no manager notification.

This applies to everyone immediately, regardless of tenure.
