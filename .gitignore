# Per-run generated state from create_agent.py / stretch_memory_curator.py
# Every person who runs these scripts gets their own IDs; committing them
# just causes collisions between teammates' local setups.
.agent_id
.environment_id
.memory_store_id
.curator_agent_id

# Scratch output from run_session_*.py
outputs/

.venv/
